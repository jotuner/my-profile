{
  "name": "folio-zhiyu-portfolio",
  "version": "1.0.0",
  "description": "张箐颐个人作品集 — 视觉设计与营销传播",
  "type": "module",
  "scripts": {
    "dev": "vite --host --open",
    "build": "vite build",
    "preview": "vite preview --host --open"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
